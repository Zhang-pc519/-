从键盘赋值建立一链表，其结点数据包括学生的学号、成绩，要求在任意顺序赋值情况下链表中结点的数学为按成绩排序。
#include<stdio.h>
#include<stdlib.h>
#define LEN sizeof(struct student)
struct student
{
	long num ;
	float score;
	struct student *next;
};
struct student *insert(struct *head,struct student *stud);
int main()
{
	struct student *head,*p1,*p2;
	long kk;
	float mark;
	int n;
	printf("\nPlease input num score:(0 is end)\n");
	head=(struct student *)malloc(LEN);
	scanf("%ld%f",&kk,&mark);
	head->num=kk;
	head->score=mark;
	head->next=NULL;
	do
	{
		p1=(struct student *)malloc(LEN);
		scanf("%ld%f",&kk,&mark);
		p1->num=kk;
	p1->score=mark;
	p1->next=NULL;
	if(p1->num!=0)
		head=insert(head,p1);
	}
	while (p1->num!=0);
	p1=head;
	printf("\nThe sorted is\n");
	printf("num    score\n");
	while (p1!=NULL)
	{
		printf("%7ld  %3.2f\n",p1->num,p1->score);
		p1=p1->next;
	}
	printf("\n");
	return 0;
}
struct student *insert(struct *head,struct student *stud);//由小到大插入函数
{
	struct student *p0,*p1,*p2;
	p1=head;
	p0=stud;
	if(head==NULL)
		head=p0;
	else
		while((p0->score>p1->score)&&(p1->next!=NULL))
		{
			p2=p1;
			p1=p1->next;
		}
		if(p0->score<=p1->score)
		{
			if(head==p1) head=p0;
			else  p2->next=p0;
			p0->next=p1
		}
		else
		{
			p1->next=p0;
			p0->next=NULL;
		}
	return (head);
}
